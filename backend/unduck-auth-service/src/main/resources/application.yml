server:
  port: 8081

spring:
  application:
    name: unduck-auth-service
  profiles:
    active: local
  config:
    import: configserver:http://localhost:8888
  cloud:
    config:
      fail-fast: true