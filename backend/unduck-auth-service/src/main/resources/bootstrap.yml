spring:
  application:
    name: unduck-auth-service
  cloud:
    config:
      uri: http://config:8888     # 도커 네트워크 상 Config Server 주소
      fail-fast: true
      retry:
        max-attempts: 5
        initial-interval: 1000
