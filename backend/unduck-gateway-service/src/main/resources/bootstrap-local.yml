spring:
  application:
    name: unduck-gateway-service

  config:
    activate:
      on-profile: local
    import: optional:configserver:http://localhost:8888   # ✅ 추가된 핵심 라인

  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 5
        max-interval: 2000
        initial-interval: 1000
