spring:
  application:
    name: unduck-platform-service

  config:
    activate:
      on-profile: local
    import: optional:configserver:http://localhost:8888

  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 5
        max-interval: 2000
        initial-interval: 1000
