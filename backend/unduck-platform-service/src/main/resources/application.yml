server:
  port: 8082

spring:
  application:
    name: unduck-platform-service

  config:
    import: "optional:configserver:http://config:8888"

management:
  endpoint:
    health:
      show-details: always